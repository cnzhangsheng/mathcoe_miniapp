/* pages/exam-report/exam-report.wxss */

.report-page {
  min-height: 100vh;
  background-color: #F8F8F8;
  padding: 24rpx;
  padding-bottom: 160rpx;
}

/* Header Title */
.report-header {
  text-align: center;
  padding: 24rpx 16rpx;
  margin-bottom: 16rpx;
}

.report-title {
  font-size: 36rpx;
  color: rgba(0,0,0,0.9);
  display: block;
}

/* Score Card */
.score-card {
  background-color: #fff;
  border-radius: 32rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);
  border: 2rpx solid rgba(0,0,0,0.05);
  margin-bottom: 16rpx;
}

.score-main {
  text-align: center;
  padding: 16rpx 0 24rpx;
  border-bottom: 2rpx solid rgba(0,0,0,0.05);
  margin-bottom: 16rpx;
}

.score-big-label {
  font-size: 24rpx;
  color: rgba(0,0,0,0.5);
  display: block;
  margin-bottom: 8rpx;
}

.score-big-value {
  font-size: 72rpx;
  color: #07C160;
}

/* Stats Grid - 4 columns */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12rpx;
}

.stat-item {
  text-align: center;
  padding: 16rpx 8rpx;
  background-color: rgba(0,0,0,0.02);
  border-radius: 16rpx;
}

.correct-item {
  background-color: rgba(7,193,96,0.1);
}

.wrong-item {
  background-color: rgba(239,68,68,0.1);
}

.stat-icon {
  font-size: 28rpx;
  display: block;
  margin-bottom: 4rpx;
}

.stat-value {
  font-size: 32rpx;
  color: rgba(0,0,0,0.8);
  display: block;
  margin-bottom: 2rpx;
}

.correct-item .stat-value {
  color: #07C160;
}

.wrong-item .stat-value {
  color: #EF4444;
}

.stat-label {
  font-size: 20rpx;
  color: rgba(0,0,0,0.4);
}

/* Answer Sheet Card */
.answer-sheet-card {
  background-color: #fff;
  border-radius: 32rpx;
  padding: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);
  border: 2rpx solid rgba(0,0,0,0.05);
}

.sheet-title-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid rgba(0,0,0,0.05);
}

.sheet-title-left {
  display: flex;
  align-items: baseline;
}

.sheet-card-title {
  font-size: 28rpx;
  color: rgba(0,0,0,0.8);
}

.sheet-tip {
  font-size: 22rpx;
  color: rgba(0,0,0,0.4);
  margin-left: 8rpx;
}

.sheet-legend {
  display: flex;
  gap: 12rpx;
}

.legend-tag {
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 12rpx;
}

.correct-tag {
  background-color: rgba(7,193,96,0.15);
  color: #07C160;
}

.wrong-tag {
  background-color: rgba(239,68,68,0.15);
  color: #EF4444;
}

/* Answer Result Grid - 5 columns */
.answer-result-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12rpx;
}

.answer-result-cell {
  aspect-ratio: 1;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cell-correct {
  background-color: rgba(7,193,96,0.15);
  border: 2rpx solid rgba(7,193,96,0.3);
}

.cell-wrong {
  background-color: rgba(239,68,68,0.15);
  border: 2rpx solid rgba(239,68,68,0.3);
}

.cell-num {
  font-size: 26rpx;
}

.cell-correct .cell-num {
  color: #07C160;
}

.cell-wrong .cell-num {
  color: #EF4444;
}

/* Bottom Action Bar */
.bottom-action-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 120rpx;
  background-color: #fff;
  display: flex;
  gap: 16rpx;
  padding: 16rpx 24rpx;
  box-shadow: 0 -2rpx 8rpx rgba(0,0,0,0.05);
  z-index: 100;
}

.action-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  border-radius: 20rpx;
  padding: 16rpx 20rpx;
}

.back-btn {
  background-color: #576B95;
}

.back-btn .action-text {
  color: #fff;
}

.retry-btn {
  background-color: #07C160;
}

.retry-btn .action-text {
  color: #fff;
}

.action-icon {
  font-size: 24rpx;
}

.action-text {
  font-size: 24rpx;
}

/* Utility Classes */
.text-bold {
  font-weight: bold;
}